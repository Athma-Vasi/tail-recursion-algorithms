// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Core__List from "@rescript/core/src/Core__List.res.js";
import * as Core__Array from "@rescript/core/src/Core__Array.res.js";

function sumRecursive(numbers) {
  var _accumulator = 0;
  var _numbers = numbers;
  while(true) {
    var numbers$1 = _numbers;
    var accumulator = _accumulator;
    var length = numbers$1.length;
    if (length === 0) {
      return accumulator;
    }
    var number = numbers$1[0];
    var head = number !== undefined ? number : 0;
    var tail = numbers$1.slice(1, length);
    _numbers = tail;
    _accumulator = head + accumulator | 0;
    continue ;
  };
}

function sumListRecursive(numbers) {
  var _accumulator = 0;
  var _numbers = numbers;
  while(true) {
    var numbers$1 = _numbers;
    var accumulator = _accumulator;
    var length = Core__List.length(numbers$1);
    if (length === 0) {
      return accumulator;
    }
    var number = Core__List.head(numbers$1);
    var head = number !== undefined ? number : 0;
    var rest = Core__List.tail(numbers$1);
    var tail = rest !== undefined ? rest : Core__List.make(0, -1);
    _numbers = tail;
    _accumulator = head + accumulator | 0;
    continue ;
  };
}

function sumIterative(numbers) {
  return Core__Array.reduce(numbers, 0, (function (accumulator, number) {
                return accumulator + number | 0;
              }));
}

var numbers = [
  1,
  2,
  3,
  4,
  5,
  7,
  8,
  9,
  2
];

var result = sumRecursive(numbers);

console.log("sum", result);

var result2 = sumIterative(numbers);

console.log("sum", result2);

var numbersList = Core__List.fromArray(numbers);

console.log("numbersList", numbersList);

var result3 = sumListRecursive(numbersList);

console.log("sum", result3);

export {
  sumRecursive ,
  sumListRecursive ,
  sumIterative ,
  numbers ,
  result ,
  result2 ,
  numbersList ,
  result3 ,
}
/* result Not a pure module */
