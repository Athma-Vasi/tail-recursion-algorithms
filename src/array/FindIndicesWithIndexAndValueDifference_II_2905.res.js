// Generated by ReScript, PLEASE EDIT WITH CARE


function findIndicesWithIndexAndValueDifference_II(nums, indexDifference, valueDifference) {
  var slideWindow = function (_answer, _minIdx, _maxIdx, _leftIdx, _rightIdx) {
    while(true) {
      var rightIdx = _rightIdx;
      var leftIdx = _leftIdx;
      var maxIdx = _maxIdx;
      var minIdx = _minIdx;
      var answer = _answer;
      if (rightIdx === nums.length) {
        return answer;
      }
      var n = nums.at(leftIdx);
      var leftNum = n !== undefined ? n : -1;
      var n$1 = nums.at(minIdx);
      var minNum = n$1 !== undefined ? n$1 : -1;
      var n$2 = nums.at(maxIdx);
      var maxNum = n$2 !== undefined ? n$2 : -1;
      var n$3 = nums.at(rightIdx);
      var rightNum = n$3 !== undefined ? n$3 : -1;
      var minDiff = minNum - rightNum | 0;
      var absMinDiff = minDiff < 0 ? Math.imul(minDiff, -1) : minDiff;
      var maxDiff = maxNum - rightNum | 0;
      var absMaxDiff = maxDiff < 0 ? Math.imul(maxDiff, -1) : maxDiff;
      var newAnswer = answer.length === 0 ? answer.concat(absMinDiff >= valueDifference ? [
                minIdx,
                rightIdx
              ] : (
                absMaxDiff >= valueDifference ? [
                    maxIdx,
                    rightIdx
                  ] : []
              )) : answer;
      _rightIdx = rightIdx + 1 | 0;
      _leftIdx = leftIdx + 1 | 0;
      _maxIdx = maxNum > leftNum ? maxIdx : leftIdx;
      _minIdx = minNum < leftNum ? minIdx : leftIdx;
      _answer = newAnswer;
      continue ;
    };
  };
  var answer = slideWindow([], 0, 0, 0, indexDifference);
  if (answer.length === 0) {
    return [
            -1,
            -1
          ];
  } else {
    return answer;
  }
}

var n1 = [
  5,
  1,
  4,
  1
];

var r1 = findIndicesWithIndexAndValueDifference_II(n1, 2, 4);

console.log("[5,1,4,1], 2, 4", r1);

var n2 = [
  2,
  1
];

var r2 = findIndicesWithIndexAndValueDifference_II(n2, 0, 0);

console.log("[2,1], 0, 0", r2);

var n3 = [
  1,
  2,
  3
];

var r3 = findIndicesWithIndexAndValueDifference_II(n3, 2, 4);

console.log("[1,2,3], 2, 4", r3);

var i1 = 2;

var v1 = 4;

var i2 = 0;

var v2 = 0;

var i3 = 2;

var v3 = 4;

export {
  findIndicesWithIndexAndValueDifference_II ,
  n1 ,
  i1 ,
  v1 ,
  r1 ,
  n2 ,
  i2 ,
  v2 ,
  r2 ,
  n3 ,
  i3 ,
  v3 ,
  r3 ,
}
/* r1 Not a pure module */
