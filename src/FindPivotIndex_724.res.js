// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Core__Array from "@rescript/core/src/Core__Array.res.js";

function findPivotIndex(numbers) {
  var length = numbers.length;
  var total = Core__Array.reduce(numbers, 0, (function (acc, curr) {
          return acc + curr | 0;
        }));
  var _pointer = 0;
  var _leftSum = 0;
  while(true) {
    var leftSum = _leftSum;
    var pointer = _pointer;
    var num = numbers[pointer - 1 | 0];
    var leftNum = num !== undefined ? num : 0;
    var num$1 = numbers[pointer];
    var currentNum = num$1 !== undefined ? num$1 : 0;
    var newLeftSum = leftSum + leftNum | 0;
    var rightSum = (total - currentNum | 0) - newLeftSum | 0;
    if (newLeftSum === rightSum) {
      return pointer;
    }
    if (pointer === (length - 1 | 0)) {
      return -1;
    }
    _leftSum = newLeftSum;
    _pointer = pointer + 1 | 0;
    continue ;
  };
}

var nums1 = [
  1,
  7,
  3,
  6,
  5,
  6
];

var result1 = findPivotIndex(nums1);

console.log("[1,7,3,6,5,6]", result1);

export {
  findPivotIndex ,
  nums1 ,
  result1 ,
}
/* result1 Not a pure module */
