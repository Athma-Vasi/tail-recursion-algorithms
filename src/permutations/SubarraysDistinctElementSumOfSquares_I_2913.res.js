// Generated by ReScript, PLEASE EDIT WITH CARE


function subarraysDistinctElementSumOfSquares_I(nums) {
  var length = nums.length;
  var _sum = 0;
  var _outerIndex = 0;
  while(true) {
    var outerIndex = _outerIndex;
    var sum = _sum;
    if (outerIndex === length) {
      return sum;
    }
    var innerLoop = function (_innerSum, subSet, _innerIndex) {
      while(true) {
        var innerIndex = _innerIndex;
        var innerSum = _innerSum;
        if (innerIndex === length) {
          return innerSum;
        }
        var n = nums.at(innerIndex);
        var num = n !== undefined ? n : 0;
        subSet.add(num);
        _innerIndex = innerIndex + 1 | 0;
        _innerSum = innerSum + Math.imul(subSet.size, subSet.size) | 0;
        continue ;
      };
    };
    var innerSum = innerLoop(sum, new Set(), outerIndex);
    _outerIndex = outerIndex + 1 | 0;
    _sum = sum + innerSum | 0;
    continue ;
  };
}

var n1 = [
  1,
  2,
  1
];

var r1 = subarraysDistinctElementSumOfSquares_I(n1);

console.log("[1,2,1]", r1);

var n2 = [
  1,
  1
];

var r2 = subarraysDistinctElementSumOfSquares_I(n2);

console.log("[1,1]", r2);

export {
  subarraysDistinctElementSumOfSquares_I ,
  n1 ,
  r1 ,
  n2 ,
  r2 ,
}
/* r1 Not a pure module */
